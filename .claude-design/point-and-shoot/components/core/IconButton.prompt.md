Square icon-only button for toolbars (select/note/capture tools) and dense chrome.

```jsx
<IconButton icon={<Icon name="crosshair"/>} label="Select element" active/>
```

`active` shows the pressed/selected tool state (accent border + subtle accent fill) — used in the injected toolbar to show which tool is live.
