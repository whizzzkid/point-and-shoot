Badge: small status pill in mono type (e.g. "3 notes", "sent"). Tag: removable chip for filters/selected elements.

```jsx
<Badge tone="accent">sent</Badge>
<Tag onRemove={()=>{}}>button.cta-primary</Tag>
```
