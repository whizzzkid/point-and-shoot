Generic surface container — the base for note rows, panels, and popover bodies.

```jsx
<Card raised>...</Card>
```

`raised` adds shadow-md for floating contexts (popovers/dialogs); inline/list cards omit it and rely on the border only.
