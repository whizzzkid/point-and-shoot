Modal for confirmations (delete note, send plan to agent).

```jsx
<Dialog open title="Send to agent?" footer={<><Button variant="ghost">Cancel</Button><Button>Send</Button></>}>
  This will compile 4 notes into a plan prompt.
</Dialog>
```
