Single-line or multiline text field, used for note text and settings fields.

```jsx
<Input placeholder="What's wrong here?" multiline rows={3}/>
<Input mono value="//*[@id='card']" />
```

`mono` switches to JetBrains Mono for technical values (XPaths, URLs). Focus always shows the accent ring (`--shadow-focus`).
