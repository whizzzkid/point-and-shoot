# Assets

No logo or icon set was provided with the brief. This design system uses:
- **Icons:** [Lucide](https://lucide.dev) loaded from CDN (`unpkg.com/lucide@latest`) — stroke icons, `currentColor`. See `guidelines/icons.card.html`.
- **Logo:** `icon.svg` — an original mark made for this system (viewfinder corner-brackets + accent-blue center dot). Pair it with the "Point & Shoot" wordmark in Space Grotesk; used in all UI kit headers. Replace if the product adopts a real logo.
